# Test dependencies.
#
#   pip install -r requirements-dev.txt
#   pytest tests/ -v
#
# The pure tests need nothing running. The live tests are skipped unless a
# server is listening -- point MMU_TEST_BASE at a TEST instance, never at a
# graph you care about.

-r requirements-host.txt

# The server's own dependencies. Five regression tests import mmu_server
# directly -- the two concurrency ones and all three batched-aging ones --
# and without fastapi installed they skipped silently, here and in CI.
-r requirements.txt

pytest>=8.0
