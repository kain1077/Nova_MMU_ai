# MMU server -- installed into the Docker image by the Dockerfile.
#
# Pins are the versions this release was validated against (Python 3.11 in the
# container). Loosen them if you need to; they are pinned so that a fresh build
# a year from now behaves like the one that was tested.

fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
neo4j==6.3.0
pdfplumber==0.11.10
